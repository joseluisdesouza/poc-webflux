spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  data.mongodb:
    host: localhost
    port: 27017
    database: local

server:
  port: 8081

---
spring:
  profiles: test
  data.mongodb:
    host: localhost
    port: 27017
    database: local

server:
  port: 8081